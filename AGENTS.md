# Agent instructions

Use English. Read `prompts/CONTINUE.md`, `docs/current/STATUS.md`, `docs/PROVENANCE.md` and the selected profile before changing runtime behavior. Historical upstream setup scripts are not the recommended installer for this continuation.

Preserve working display, native refresh rate, NVIDIA workaround, game and input paths. Tracking is not physically accepted. Never install a rejected backend or turn on experimental calibration/bias options merely because they compile. Do not use OpenVR GL mirror-texture APIs on the reference machine. Do not alter live VR for documentation/packaging tasks.

Public files must contain no private room recordings, serials, unit calibration, registry data, Steam account data, credentials, personal contact information or machine-specific home paths. Keep private evidence outside Git. Use a public handle and noreply Git author identity. Windows installations are read-only references.

Document one falsifiable change at a time, relevant tests and limits, baseline/current hashes, rollback and user acceptance. Static tests cannot establish moving-head comfort. Consult the detailed continuation prompt for frame/timestamp/fusion and release requirements.
